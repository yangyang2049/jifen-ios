# 14 张截图的二次复核与修复（2026-09-14）

本轮以用户逐图反馈为准，先与上一轮最终代码及截图比较，再修复仍存在的问题。参考本地 Android/HarmonyOS 实现；只验横屏显示端。未修改 Android/HarmonyOS 仓库。

## 逐项结论

| 反馈 | 二次复核与处理 |
|---|---|
| 图 1–2：拳击加按钮位置、图标及菜单层级 | 确认仍存在。将回合标签和加按钮移入 `ScoreboardTemplate.contentOverlayProvider`，位于公共菜单下方；编辑及样式面板打开时隐藏。按钮改 SF Symbols `plus`，尺寸和底部间距采用斗地主公共常量，底部正中。 |
| 图 3：黑八编辑时目标局数 | 确认仍存在。公共 Scaffold 默认隐藏不可编辑顶栏；仅斯诺克可编辑标题显式保留。普通编辑、样式编辑和旧样式面板均遵循该规则。 |
| 图 4：足球计时条右侧补时入口 | 确认仍存在。计时条移入公共辅助层，编辑时整组隐藏；删除右侧加减菜单。按 Android 的菜单结构将“伤停补时”放入主菜单，保留增加及撤销补时的设置面板。五人制仍保留正常计时/暂停。 |
| 图 5：网球类编辑局盘布局 | 确认仍存在。网球、板式网球、软式网球及双打共用每侧两列并排的局/盘编辑行，列标题在数值上方；移除中缝共享标签。网球菜单抬到本页辅助层之上。 |
| 图 6：斯诺克大小分间距及底栏 | 确认仍存在。原 8 pt 间距改用随半屏宽度变化的公共间距（28–72 pt）。样式编辑时底部进球、犯规、换手面板不渲染，普通编辑亦隐藏；保留可编辑标题及分数。 |
| 图 7：UNO 目标 Chip | 确认旧样式设置面板打开时仍显示；现已隐藏。 |
| 图 8–10：匹克球、羽毛球双打、板网球灰底 | 这三张来自独立样式组件测试。`testAuditStylePanelSnapshots` 明确使用 `Color.gray` 作为宿主，并未嵌入计分板。它们不证明真实页面未渲染；新图库单独展示实际原生页面，不混入灰底组件图。 |
| 图 11：射箭和双打缺指示器 | 发送端已提供 `archeryCurrentShooter`、双打 `isServer`，显示端也已读取。原审计合成数据未填发球/射手状态；已补齐并复核横屏渲染，避免把不完整的测试数据误判为产品缺功能。 |
| 图 12：名称和分数布局靠底 | 上一轮已修复。台球、足球、五人制足球、简单计分等采用名称与分数整体居中；本轮继续覆盖手机/平板横屏投屏与同步显示，未重复改写这部分。 |
| 图 13：多人默认分数黑字 | 确认仍存在。多人渲染器缺省主分改白色；进一步修复篮球、三人篮球、九球旧默认红/绿面板错误选黑字。九球追分条的统计数字也补为白色，并增加像素回归。自定义文字色及自定义背景的自动对比逻辑保留，有专项回归。 |
| 图 14：双打底部姓名出屏 | 上一轮已修复姓名区内边距及高度预算。本轮复查 1.5 倍横屏截图，底部姓名完整，未重复修改。 |
| 多人一位数过大 | 本机多人字号先受公共响应式基准上限约束，再应用倍率并按可用宽高收缩；长分数继续自动缩小。显示端参考 Android/HarmonyOS 多人格基准 36–120，再应用同步倍率与空间约束。 |

另外统一检查并补修：球拍类关键分提示及乒乓球裁判标记、网球抢七提示、篮球不可编辑计时区、九球不可编辑统计区在编辑/样式设置时的显示条件。发球指示器本身具有颜色编辑能力的页面保留其样式预览。

## 对照范围与证据

- Android：`ScoreboardResponsive.kt`、`MultiScoreLayoutHelpers.kt`、`DisplayTypographyResolver.kt`、`S1DualSideScoreRouteScreen.kt`（补时主菜单及独立设置）、`SnookerScoreScreen.kt`。
- HarmonyOS：`scoreboard/display/displayTypography.ts`、`pages/scoreboard/sports/SnookerScoreboard.ets`。
- 所有 33 个显示类型均经过生产 SwiftUI 渲染器：852×393、1194×834 横屏，投屏/同步两类，1/1.5/2 倍输入共 396 张。2 倍为越界压力输入，输出仍遵守产品倍率上限。
- 目录原生专项涵盖 17 个受影响项目，以及乒乓球、羽毛球、网球、匹克球四类双打；包含计分、普通编辑、菜单、样式编辑、字体/背景/主题面板。另有拳击按钮坐标和补时加减操作专项。
- 补充九球、多人计分、UNO 的三人/四人单行布局，包含一位数、三位数及负数。
- 源码再次扫描未发现活动 `WatchConnectivity`、`WCSession`、WatchLink/WatchSession 实现；手表保持上一轮删除状态。

## 最终验证

- 干净 `build-for-testing` 成功，后续收尾增量构建成功；无新增告警类别，`git diff --check` 通过。
- `final-unit.xcresult`：原补修阶段 430 项单测、0 失败。提交前二次自检补充账号密码登录接口/输入契约测试和黑 8 让球双侧等高回归后，当前为 434 项单测、0 失败；黑 8 未使用变量告警已消除。
- `ui.xcresult`：17 个项目主流程通过（664 秒），四类双打通过（166 秒）。最终网球菜单单独复验通过。
- `controls.xcresult`：拳击按钮底部居中坐标、回合弹窗、伤停补时主菜单入口、增加一分钟及撤销操作通过。
- 新图库共 581 张：原生页面 166、横屏显示端 396、专项渲染 19。15 张重点截图另存于文档目录。
- 真实 AirPlay、Android/HarmonyOS 实机及三端云房间互联未在本轮运行；本轮证据为 iOS 原生模拟器操作、生产渲染器输出及本地源代码对照。

中途失败记录保留：新增局/盘文案最初缺少英文键，已补齐并全量通过；补时面板容器最初覆盖子按钮的无障碍标识，已修正分组并通过完整加减操作复验。这些失败不作为最终通过证据。

581 张二次复核图库与测试摘要位于本地生成目录 `build/followup-2026-09-14/`（不随仓库提交）；[重点截图](audits/2026-09-14/followup)随报告持久保存。

原始日志、失败证据及复验结果保存在 `build/followup-2026-09-14/`；开始前差异备份为 `baseline.patch`。本报告生成阶段未发布。
