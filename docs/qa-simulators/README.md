# QA simulators (offline)

These Swift scripts are **not** part of any Xcode target or SPM package.
They are kept for offline rule simulation / historical reference only.

Note: `watch_scoreboard_simulation.swift` still references deleted legacy
`Watch*Rules` types. Prefer `Packages/JifenCore` tests and live
`WatchRallyScoreView` paths for current behavior.
