# QA simulators (offline)

These Swift scripts are **not** part of any Xcode target or SPM package.
They are kept for offline rule simulation / historical reference only.

The obsolete Watch rule simulation was removed with the iOS Watch integration on
2026-09-14. Use `Packages/JifenCore` tests and iOS scoreboard regression tests for
current behavior.
